Lines distorted to look like a blob.
===========

```bash
(target="hatched-blob"; ghcid --command='stack ghci generative-art:lib '"$target"' --main-is '"$target" --test=main --warnings --no-title)
```
